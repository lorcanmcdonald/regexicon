machine:
  ghc:
  version: 7.10.3
  services:
    - docker

dependencies:
  cache_directories:
    - ./obj
compile:
  override:
    - ./script/build

deployment:
  live:
    branch: live
    commands:
      - ./script/deploy
