machine:
  ghc:
  version: 7.10.3
  services:
    - docker

dependencies:
  override:
    - "true"
  cache_directories:
    - ./obj
compile:
  override:
    - ./script/build
test:
  override:
    - "true"

deployment:
  live:
    branch: live
    commands:
      - ./script/deploy
